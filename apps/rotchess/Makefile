APP := rotchess
APP_DISPLAY_NAME := ROT Chess
APP_CMAKE_OPTION := ROTTOOLS_BUILD_ROTCHESS
OTHER_APP_CMAKE_OPTIONS := -DROTTOOLS_BUILD_ROTREADER=OFF -DROTTOOLS_BUILD_ROTPAD=OFF
PROJECT_ROOT := $(abspath $(dir $(lastword $(MAKEFILE_LIST)))/../..)

include $(PROJECT_ROOT)/make/App.mk
