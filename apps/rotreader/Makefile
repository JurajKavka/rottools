APP := rotreader
APP_DISPLAY_NAME := ROT Reader
APP_CMAKE_OPTION := ROTTOOLS_BUILD_ROTREADER
OTHER_APP_CMAKE_OPTIONS := -DROTTOOLS_BUILD_ROTPAD=OFF -DROTTOOLS_BUILD_ROTFM=OFF
PROJECT_ROOT := $(abspath $(dir $(lastword $(MAKEFILE_LIST)))/../..)

include $(PROJECT_ROOT)/make/App.mk
