APP := rotpad
APP_DISPLAY_NAME := ROT Pad
APP_CMAKE_OPTION := ROTTOOLS_BUILD_ROTPAD
OTHER_APP_CMAKE_OPTIONS := -DROTTOOLS_BUILD_ROTREADER=OFF -DROTTOOLS_BUILD_ROTFM=OFF
PROJECT_ROOT := $(abspath $(dir $(lastword $(MAKEFILE_LIST)))/../..)

include $(PROJECT_ROOT)/make/App.mk
